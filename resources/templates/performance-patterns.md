# Performance Patterns — hardened lessons

Last updated: {date}. Each row was paid for with a real regression. Append via the maintenance loop; never delete a row without checking the trap is structurally gone. NOT a scoreboard — capture the transferable pattern, not "do more of what won once."

| Pattern | Evidence | How to use it |
|---------|----------|---------------|
| {trap or win pattern} | {date + what happened} | {the behavior change} |
