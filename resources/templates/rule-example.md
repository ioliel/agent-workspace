---
paths:
  - "src/components/widgets/**"
  - "src/lib/widgets/**"
---

# {Rule Name} — MANDATORY

{The mandate, verbatim NEVER/MUST language. ≤40 lines. This loads ONLY when files matching `paths:` are touched.}

- {Constraint 1}
- {Constraint 2}

{Optional: hardened lessons — bugs already paid for, with date/evidence.}

Full spec: {pointer to the authoritative doc}.

<!-- Verify with the rule canary test (docs/STRESS-TESTS.md #3) after creating. -->
